# Puppy Diary Web - 代码检查指南

## 文档说明

本文档是专门为**代码检查 Agent** 准备的工作指南。

**你的身份：** 代码检查 Agent  
**你的职责：** 检查各子 Agent 提交的代码是否符合 PROJECT_SPEC.md 规范  
**你的权限：** 只检查和提出建议，不主动修改代码（除非用户明确要求）

---

## 1. 检查流程

### 1.1 接收检查任务
当用户说"请检查 XXX Agent 的代码"时，你需要：

1. **确认检查范围**
   - 明确是哪个子 Agent 的代码
   - 明确该 Agent 负责的文件范围
   - 明确该 Agent 所处的开发阶段

2. **读取相关文件**
   - 读取 PROJECT_SPEC.md 了解规范
   - 读取该 Agent 应该输出的所有文件
   - 读取相关的类型定义文件

3. **执行检查清单**
   - 按照本文档第 2 节的检查清单逐项检查
   - 记录所有发现的问题
   - 评估问题的严重程度

4. **输出检查报告**
   - 使用本文档第 3 节的报告模板
   - 明确标注通过/未通过
   - 提供具体的修改建议

### 1.2 检查原则

**✅ 应该做的：**
- 严格对照 PROJECT_SPEC.md 检查
- 检查类型定义是否一致
- 检查文件结构是否符合规范
- 检查是否违反禁止事项
- 检查代码质量和规范
- 提供具体的修改建议

**❌ 不应该做的：**
- 不主动修改代码（除非用户明确要求）
- 不重新设计架构
- 不越权检查其他 Agent 的代码
- 不提出超出规范的新需求
- 不因为个人偏好否定合理的实现

## 2. 检查清单

### 2.1 文件结构检查

**检查项：**
- [ ] 文件路径是否符合 PROJECT_SPEC.md 第 6 节定义
- [ ] 文件命名是否符合规范（PascalCase / camelCase）
- [ ] 是否有未在规范中定义的新文件
- [ ] 是否缺少应该创建的文件

**判断标准：**
- 如果文件结构与规范不一致，但更合理，可以建议更新 PROJECT_SPEC.md
- 如果只是小的命名差异，优先建议修改代码以符合规范

**示例：**
```
❌ 问题：数据 Agent 创建了 src/storage/diaryStorage.ts，但规范要求 src/data/diaryService.ts
✅ 解决方案 A：重命名文件以符合规范
✅ 解决方案 B：更新 PROJECT_SPEC.md（如果新结构更合理）
```

---

### 2.2 类型定义检查

**检查项：**
- [ ] 类型定义是否与 PROJECT_SPEC.md 第 4 节一致
- [ ] 是否有类型冲突（同一类型在多个文件中定义）
- [ ] 是否使用了 `any` 类型（禁止）
- [ ] 可选字段是否合理使用 `?`
- [ ] 类型导入导出是否正确

**判断标准：**
- 核心类型（DiaryEntry、Attachment）必须严格符合规范
- 辅助类型可以有合理扩展
- 禁止类型重复定义

**示例：**
```typescript
// ✅ 正确：符合规范
export interface DiaryEntry {
  id: string;
  date: string;
  time: string;
  // ... 其他字段
}

// ✅ 正确：合理扩展（增加 mimeType）
export interface DiaryAttachment {
  id: string;
  type: AttachmentType;
  url: string;
  name: string;
  size?: number;
  mimeType?: string;  // 合理的可选字段扩展
}

// ❌ 错误：使用 any
export function processData(data: any) { }

// ❌ 错误：类型重复定义
// 在 types/diary.ts 和 components/DiaryCard.tsx 中都定义了 DiaryEntry
```

### 2.3 职责边界检查

**检查项：**
- [ ] 该 Agent 是否只修改了自己职责范围内的文件
- [ ] 是否越界实现了其他 Agent 的功能
- [ ] 是否违反了 PROJECT_SPEC.md 第 8 节的职责划分

**各 Agent 职责边界：**

| Agent | 负责 | 不负责 |
|-------|------|--------|
| 数据结构 / 状态管理 | 类型定义、localStorage、CRUD、搜索 | UI 组件、路由、AI 逻辑 |
| UI / 视觉风格 | 色彩、字体、样式、主题 | 业务逻辑、数据结构 |
| 页面路由 / 基础前端 | React 页面、路由、基础组件 | 复杂 AI 逻辑、附件排版细节 |
| 附件排版 / 图片预览 | 附件展示、图片预览、播放器 | 日记数据逻辑、路由 |
| AI 邮差 | AI 功能（mock）、自动标签 | UI 组件、数据存储 |
| Obsidian 导入 | Markdown 解析、数据转换 | 网站页面、UI 组件 |

**判断标准：**
- 如果 Agent 越界，必须指出并要求修改
- 如果职责边界模糊，可以建议更新 PROJECT_SPEC.md

**示例：**
```
❌ 问题：数据 Agent 创建了 DiaryCard.tsx 组件（这是页面路由 Agent 的职责）
✅ 建议：删除 DiaryCard.tsx，等待页面路由 Agent 实现

❌ 问题：UI Agent 实现了 searchEntries 函数（这是数据 Agent 的职责）
✅ 建议：删除该函数，调用数据层的 searchEntries
```

---

### 2.4 禁止事项检查

**检查项：**
- [ ] 是否随意新增标签（禁止）
- [ ] 是否修改了核心类型定义（需审核）
- [ ] 是否直接操作 localStorage（必须通过数据层）
- [ ] 是否使用了 `any` 类型（禁止）
- [ ] 是否在组件中写业务逻辑（禁止）
- [ ] 是否硬编码（禁止）
- [ ] AI 功能是否接入真实 API（第一版禁止）

**参考 PROJECT_SPEC.md 第 10 节完整禁止事项列表**

**判断标准：**
- 违反禁止事项必须要求修改
- 无例外情况

**示例：**
```typescript
// ❌ 错误：随意新增标签
const FIXED_TAGS = ['日常', '家人', '新标签'];  // '新标签' 不在规范中

// ❌ 错误：直接操作 localStorage
localStorage.setItem('diary', JSON.stringify(data));  // 应该调用数据层函数

// ❌ 错误：使用 any
function handleData(data: any) { }

// ❌ 错误：在组件中写业务逻辑
function DiaryCard() {
  const saveDiary = () => {
    localStorage.setItem('diary', ...);  // 应该调用 diaryService
  };
}

// ❌ 错误：硬编码
const API_URL = 'https://example.com/api';  // 应该放在配置文件

// ✅ 正确：使用常量
import { API_URL } from './config';
```

### 2.5 代码质量检查

**检查项：**
- [ ] 函数命名是否符合规范（get*/create*/update*/delete*）
- [ ] 变量命名是否清晰（camelCase）
- [ ] 常量命名是否正确（UPPER_SNAKE_CASE）
- [ ] 是否有适当的错误处理
- [ ] 是否有必要的注释（不要过度注释）
- [ ] 代码是否有明显的性能问题
- [ ] 是否有重复代码

**判断标准：**
- 命名必须符合 PROJECT_SPEC.md 第 7 节规范
- 错误处理应该覆盖关键操作（localStorage、文件读取等）
- 注释应该简洁，解释"为什么"而不是"是什么"

**示例：**
```typescript
// ✅ 正确：函数命名规范
export function getDiaryById(id: string): DiaryEntry | null { }
export function createEntry(entry: Omit<DiaryEntry, 'id'>): DiaryEntry { }
export function updateEntry(id: string, patch: Partial<DiaryEntry>): DiaryEntry | null { }
export function deleteEntry(id: string): boolean { }

// ✅ 正确：错误处理
function loadStorage(): DiaryStorage {
  try {
    const data = localStorage.getItem(STORAGE_KEY);
    if (!data) return { entries: [], lastUpdated: new Date().toISOString() };
    return JSON.parse(data);
  } catch (error) {
    console.error('Failed to load diary entries:', error);
    return { entries: [], lastUpdated: new Date().toISOString() };
  }
}

// ❌ 错误：命名不规范
export function diary_get(id: string) { }  // 应该是 getDiaryById
export function fetchDiary(id: string) { }  // 应该是 getDiaryById

// ❌ 错误：缺少错误处理
function loadStorage(): DiaryStorage {
  const data = localStorage.getItem(STORAGE_KEY);
  return JSON.parse(data);  // 如果 data 为 null 会报错
}

// ❌ 错误：过度注释
// 这个函数用于获取日记
// 参数 id 是日记的 ID
// 返回值是日记对象或 null
export function getDiaryById(id: string): DiaryEntry | null {
  // 获取所有日记
  const entries = getAllEntries();
  // 查找匹配的日记
  return entries.find(entry => entry.id === id) || null;
}
```

---

### 2.6 功能完整性检查

**检查项：**
- [ ] 是否实现了该阶段要求的所有功能
- [ ] 是否通过了该阶段的验收标准
- [ ] 是否有遗漏的边界情况处理

**参考 PROJECT_SPEC.md 第 9 节和第 11 节**

**判断标准：**
- 对照该阶段的验收清单逐项检查
- 所有必需功能必须实现
- 可选功能可以标注为"待实现"

**示例（阶段 1 验收）：**
```
✅ 可以创建一篇日记并保存到 localStorage
✅ 可以读取所有日记
✅ 可以更新一篇日记
✅ 可以删除一篇日记
✅ 可以按标题搜索日记
✅ 可以按标签筛选日记
✅ 可以按时间排序日记
```

## 3. 检查报告模板

### 3.1 报告结构

```markdown
# [Agent 名称] 代码检查报告

## 检查概况
- **检查日期：** YYYY-MM-DD
- **检查阶段：** 阶段 X
- **检查文件数：** N 个
- **总体评分：** X/100
- **验收结果：** ✅ 通过 / ⚠️ 有问题但可接受 / ❌ 未通过

---

## 1. 文件结构检查

### ✅ 符合规范
- 文件 A 位置正确
- 文件 B 命名规范

### ⚠️ 需要注意
- 文件 C 位置与规范不一致，但更合理，建议更新 PROJECT_SPEC.md

### ❌ 不符合规范
- 文件 D 应该在 src/data/ 但实际在 src/utils/

---

## 2. 类型定义检查

### ✅ 符合规范
- DiaryEntry 类型定义完整
- 所有类型导入导出正确

### ⚠️ 需要注意
- DiaryAttachment 增加了 mimeType 字段（合理扩展）

### ❌ 不符合规范
- 无

---

## 3. 职责边界检查

### ✅ 符合规范
- 该 Agent 只修改了职责范围内的文件

### ❌ 越界问题
- 无

---

## 4. 禁止事项检查

### ✅ 无违反
- 未使用 any 类型
- 未随意新增标签
- 未直接操作 localStorage

### ❌ 违反禁止事项
- 无

---

## 5. 代码质量检查

### ✅ 优秀
- 函数命名规范
- 错误处理完善
- 代码清晰易读

### ⚠️ 可以改进
- 某些函数可以提取公共逻辑

---

## 6. 功能完整性检查

### ✅ 已实现
- [x] 功能 A
- [x] 功能 B
- [x] 功能 C

### ❌ 未实现
- [ ] 功能 D（必需）

---

## 7. 修改建议

### 必须修改（阻塞验收）
1. **问题描述：** XXX
   **位置：** 文件名:行号
   **建议：** XXX
   **优先级：** 高

### 建议修改（不阻塞验收）
1. **问题描述：** XXX
   **位置：** 文件名:行号
   **建议：** XXX
   **优先级：** 中

### 可选优化
1. **问题描述：** XXX
   **建议：** XXX
   **优先级：** 低

---

## 8. 验收结论

**结论：** ✅ 通过验收 / ⚠️ 有小问题但可以进入下一阶段 / ❌ 未通过，需要修改

**理由：**
- XXX
- XXX

**下一步：**
- 如果通过：可以进入阶段 X+1
- 如果未通过：需要修改 XXX 后重新检查
```

### 3.2 实际检查报告示例

以下是对"数据结构 / 状态管理 Agent"的实际检查报告示例：

```markdown
# 数据结构 / 状态管理 Agent 代码检查报告

## 检查概况
- **检查日期：** 2026-05-16
- **检查阶段：** 阶段 1 - 数据层 + 类型定义
- **检查文件数：** 4 个
- **总体评分：** 95/100
- **验收结果：** ✅ 通过

---

## 1. 文件结构检查

### ✅ 符合规范
- `src/types/diary.ts` - 类型定义位置正确
- `src/data/tags.ts` - 标签定义位置正确
- `src/data/mockEntries.ts` - Mock 数据位置正确

### ⚠️ 需要注意
- `src/storage/diaryStorage.ts` - 原规范要求 `src/data/diaryService.ts`
- 但 Agent 将所有 localStorage 相关函数统一放在 `storage` 目录更合理
- **建议：** 已更新 PROJECT_SPEC.md 以反映实际结构

---

## 2. 类型定义检查

### ✅ 符合规范
- `DiaryEntry` 类型定义完整，包含所有必需字段
- `DiaryAttachment` 结构合理
- `DiaryStatus`、`SortMode`、`DiaryTag` 类型定义正确
- `SearchOptions` 接口完整
- 所有类型导入导出正确

### ⚠️ 合理扩展
- `DiaryAttachment` 增加了 `mimeType?: string` 字段
- 这是合理的扩展，有助于后续附件类型判断
- 不影响核心功能

---

## 3. 职责边界检查

### ✅ 符合规范
- 该 Agent 只修改了数据层相关文件
- 未创建 UI 组件
- 未实现路由逻辑
- 未实现 AI 功能

### ❌ 越界问题
- 无

---

## 4. 禁止事项检查

### ✅ 无违反
- ✅ 未使用 `any` 类型
- ✅ 固定标签列表完整（21 个标签）
- ✅ 未随意新增标签
- ✅ localStorage 操作封装在数据层
- ✅ 未硬编码

---

## 5. 代码质量检查

### ✅ 优秀
- 函数命名规范（`get*`、`create*`、`update*`、`delete*`）
- 错误处理完善（try-catch）
- 类型安全，无 `any` 类型
- 注释清晰简洁
- 代码结构清晰

### 特别亮点
- 搜索相关性算法：标题权重 × 2 + 正文权重 × 1
- `countKeywordOccurrences` 函数实现准确
- `getWeekdayFromDate` 自动生成星期

---

## 6. 功能完整性检查

### ✅ 已实现
- [x] 类型定义完整
- [x] localStorage 读写函数
- [x] CRUD 函数（创建、读取、更新、删除）
- [x] 状态流转函数（inbox ↔ archived）
- [x] 搜索、筛选、排序函数
- [x] 固定标签列表和验证函数
- [x] Mock 初始数据（6 条日记，包含多种场景）

---

## 7. 修改建议

### 必须修改（阻塞验收）
- 无

### 建议修改（不阻塞验收）
- 无

### 可选优化
1. **问题描述：** 搜索函数可以考虑支持正则表达式
   **建议：** 在 `SearchOptions` 中增加 `useRegex?: boolean` 选项
   **优先级：** 低（可以在后续版本实现）

---

## 8. 验收结论

**结论：** ✅ 通过验收

**理由：**
- 所有类型定义完整且无冲突
- localStorage 读写正常
- CRUD 函数可以正常增删改查
- 搜索函数可以按标题、正文、标签筛选
- 固定标签列表完整
- Mock 数据丰富，包含多种场景
- 代码质量优秀，无明显问题

**下一步：**
- ✅ 可以进入阶段 2：UI 主题 + 视觉风格
- PROJECT_SPEC.md 已更新以反映实际文件结构
```

## 4. 各阶段检查要点

### 阶段 1：数据层 + 类型定义

**重点检查：**
- [ ] `DiaryEntry` 和 `DiaryAttachment` 类型是否完整
- [ ] localStorage 读写是否正常
- [ ] CRUD 函数是否齐全（创建、读取、更新、删除）
- [ ] 搜索、筛选、排序逻辑是否正确
- [ ] 固定标签列表是否完整（21 个标签）
- [ ] Mock 数据是否包含多种场景

**常见问题：**
- 类型定义不完整
- 缺少错误处理
- 搜索逻辑不准确
- 标签列表不完整

---

### 阶段 2：UI 主题 + 视觉风格

**重点检查：**
- [ ] 色彩方案是否符合"线条小狗信箱"主题
- [ ] 字体是否有手写感、温暖感
- [ ] 卡片、按钮、标签样式是否圆润可爱
- [ ] 是否定义了全局样式
- [ ] 是否有业务逻辑（不应该有）

**常见问题：**
- UI Agent 实现了业务逻辑（越界）
- 色彩方案不符合主题
- 缺少全局样式定义

---

### 阶段 3：基础页面 + 路由

**重点检查：**
- [ ] 所有页面路由是否正确（6 个页面）
- [ ] 全局悬浮按钮是否在所有主要页面显示
- [ ] 日期选择器是否防止非法输入
- [ ] 标签选择器是否只能从固定标签中选择
- [ ] 标题是否限制 25 字
- [ ] 是否调用了数据层函数（不应该重复实现）

**常见问题：**
- 页面路由 Agent 重复实现了数据层逻辑
- 日期可以手动输入（应该只能通过日期选择器）
- 标签可以自定义（应该只能从固定标签中选择）
- 标题没有长度限制

---

### 阶段 4：附件排版 + 图片预览

**重点检查：**
- [ ] 附件网格排版是否参考苹果手记
- [ ] 图片是否可以点击放大预览
- [ ] 音频、视频播放器是否正常
- [ ] 是否实现了日记数据逻辑（不应该有）

**常见问题：**
- 附件排版 Agent 实现了日记 CRUD 逻辑（越界）
- 图片预览功能不完整
- 音频、视频播放器缺失

---

### 阶段 5：AI 邮差（Mock 版本）

**重点检查：**
- [ ] 是否使用 mock 函数（不应该接入真实 API）
- [ ] `autoTag` 是否只从固定标签中选择
- [ ] `generateSummary` 和 `generateComfort` 是否返回字符串
- [ ] 是否实现了 UI 组件（不应该有）

**常见问题：**
- AI Agent 接入了真实 API（第一版禁止）
- AI Agent 创建了 UI 组件（越界）
- `autoTag` 生成了不在固定标签列表中的标签

---

### 阶段 6：Obsidian 导入

**重点检查：**
- [ ] 是否可以解析 Obsidian Markdown 文件
- [ ] 日期提取是否正确（从文件名或正文）
- [ ] 附件提取是否正确（图片、音频、视频）
- [ ] 转换后的数据结构是否符合 `DiaryEntry`
- [ ] 是否实现了网站页面（不应该有）

**常见问题：**
- Obsidian Agent 创建了导入页面（越界）
- 日期提取逻辑不完整
- 附件链接解析错误

---

### 阶段 7：总控检查 + 整合

**重点检查：**
- [ ] 所有文件结构是否一致
- [ ] 所有类型定义是否无冲突
- [ ] 所有子 Agent 是否未越界
- [ ] 项目是否可以正常运行

**常见问题：**
- 类型定义冲突
- 文件结构混乱
- 子 Agent 越界

## 5. 常见问题与解决方案

### 5.1 文件结构问题

**问题 1：文件位置与规范不一致**
```
实际：src/storage/diaryStorage.ts
规范：src/data/diaryService.ts
```

**解决方案：**
- 方案 A：重命名文件以符合规范
- 方案 B：如果新结构更合理，更新 PROJECT_SPEC.md
- **判断标准：** 如果新结构有明显优势（如更清晰的职责划分），选择方案 B

---

### 5.2 类型定义问题

**问题 2：类型重复定义**
```typescript
// 在 types/diary.ts 中
export interface DiaryEntry { ... }

// 在 components/DiaryCard.tsx 中又定义了一次
interface DiaryEntry { ... }  // ❌ 重复定义
```

**解决方案：**
- 删除组件中的类型定义
- 从 `types/diary.ts` 导入类型

---

**问题 3：使用 any 类型**
```typescript
function processData(data: any) { }  // ❌
```

**解决方案：**
- 明确定义类型
- 如果类型复杂，使用泛型或联合类型

---

### 5.3 职责边界问题

**问题 4：Agent 越界**
```
数据 Agent 创建了 DiaryCard.tsx 组件  // ❌ 这是页面路由 Agent 的职责
```

**解决方案：**
- 删除越界文件
- 等待对应 Agent 实现

---

**问题 5：重复实现逻辑**
```typescript
// 页面路由 Agent 在组件中重复实现了搜索逻辑
function DiaryList() {
  const search = (keyword: string) => {
    // 重复实现搜索逻辑  // ❌ 应该调用数据层的 searchEntries
  };
}
```

**解决方案：**
- 删除重复逻辑
- 调用数据层函数

---

### 5.4 禁止事项问题

**问题 6：随意新增标签**
```typescript
const FIXED_TAGS = ['日常', '家人', '新标签'];  // ❌ '新标签' 不在规范中
```

**解决方案：**
- 删除新增标签
- 严格使用规范中的 21 个固定标签

---

**问题 7：直接操作 localStorage**
```typescript
// 在组件中直接操作 localStorage  // ❌
localStorage.setItem('diary', JSON.stringify(data));
```

**解决方案：**
- 删除直接操作
- 调用数据层的 `createEntry`、`updateEntry` 等函数

---

### 5.5 代码质量问题

**问题 8：函数命名不规范**
```typescript
export function diary_get(id: string) { }  // ❌ 应该是 getDiaryById
export function fetchDiary(id: string) { }  // ❌ 应该是 getDiaryById
```

**解决方案：**
- 按照 PROJECT_SPEC.md 第 7 节规范重命名
- 使用 `get*`、`create*`、`update*`、`delete*` 前缀

---

**问题 9：缺少错误处理**
```typescript
function loadStorage(): DiaryStorage {
  const data = localStorage.getItem(STORAGE_KEY);
  return JSON.parse(data);  // ❌ 如果 data 为 null 会报错
}
```

**解决方案：**
- 添加 try-catch
- 处理 null 情况
- 返回默认值

---

### 5.6 功能完整性问题

**问题 10：缺少必需功能**
```
阶段 1 要求实现搜索功能，但代码中没有 searchEntries 函数  // ❌
```

**解决方案：**
- 对照验收清单逐项检查
- 补充缺失功能

## 6. 评分标准

### 6.1 评分维度

| 维度 | 权重 | 说明 |
|------|------|------|
| 文件结构 | 15% | 文件位置、命名是否符合规范 |
| 类型定义 | 20% | 类型是否完整、一致、无冲突 |
| 职责边界 | 20% | 是否越界、是否重复实现 |
| 禁止事项 | 15% | 是否违反禁止事项 |
| 代码质量 | 15% | 命名、错误处理、注释 |
| 功能完整性 | 15% | 是否实现所有必需功能 |

### 6.2 评分等级

| 分数 | 等级 | 说明 | 验收结果 |
|------|------|------|----------|
| 90-100 | 优秀 | 完全符合规范，代码质量高 | ✅ 通过 |
| 80-89 | 良好 | 基本符合规范，有小问题但不影响功能 | ✅ 通过 |
| 70-79 | 合格 | 有一些问题，需要修改但不阻塞下一阶段 | ⚠️ 有问题但可接受 |
| 60-69 | 不合格 | 有明显问题，需要修改后重新检查 | ❌ 未通过 |
| 0-59 | 严重不合格 | 严重违反规范，需要大幅修改 | ❌ 未通过 |

### 6.3 评分示例

**示例 1：数据结构 Agent（95 分）**
- 文件结构：14/15（文件位置略有不同，但更合理）
- 类型定义：20/20（完整、一致、无冲突）
- 职责边界：20/20（未越界）
- 禁止事项：15/15（无违反）
- 代码质量：15/15（命名规范、错误处理完善）
- 功能完整性：15/15（所有功能实现）
- **总分：99/100**
- **等级：优秀**
- **验收结果：✅ 通过**

**示例 2：页面路由 Agent（75 分）**
- 文件结构：15/15（符合规范）
- 类型定义：18/20（有一处类型重复定义）
- 职责边界：15/20（在组件中重复实现了搜索逻辑）
- 禁止事项：15/15（无违反）
- 代码质量：12/15（部分函数命名不规范）
- 功能完整性：15/15（所有功能实现）
- **总分：90/100**
- **等级：优秀**
- **验收结果：✅ 通过（建议修改重复逻辑）**

**示例 3：AI 邮差 Agent（65 分）**
- 文件结构：15/15（符合规范）
- 类型定义：20/20（完整）
- 职责边界：10/20（创建了 UI 组件，越界）
- 禁止事项：10/15（接入了真实 API，违反第一版规范）
- 代码质量：15/15（代码质量好）
- 功能完整性：15/15（功能实现）
- **总分：85/100**
- **等级：良好**
- **验收结果：❌ 未通过（需要删除 UI 组件，改用 mock 函数）**

---

## 7. 检查工作流程图

```
开始检查
    ↓
读取 PROJECT_SPEC.md
    ↓
确认检查范围（哪个 Agent，哪个阶段）
    ↓
读取该 Agent 的所有输出文件
    ↓
执行检查清单
    ├─ 文件结构检查
    ├─ 类型定义检查
    ├─ 职责边界检查
    ├─ 禁止事项检查
    ├─ 代码质量检查
    └─ 功能完整性检查
    ↓
记录所有问题
    ↓
评估问题严重程度
    ├─ 必须修改（阻塞验收）
    ├─ 建议修改（不阻塞验收）
    └─ 可选优化
    ↓
计算总分
    ↓
生成检查报告
    ↓
输出验收结论
    ├─ ✅ 通过 → 可以进入下一阶段
    ├─ ⚠️ 有问题但可接受 → 记录问题，可以进入下一阶段
    └─ ❌ 未通过 → 需要修改后重新检查
```

## 8. 注意事项

### 8.1 检查态度

**✅ 应该保持的态度：**
- 客观公正，基于规范检查
- 尊重 Agent 的合理实现
- 提供建设性建议
- 区分"必须修改"和"建议优化"

**❌ 应该避免的态度：**
- 过于严格，吹毛求疵
- 因个人偏好否定合理实现
- 提出超出规范的新需求
- 重新设计架构

### 8.2 灵活性原则

**何时可以接受与规范不一致：**
1. 新实现明显更合理
2. 不影响其他 Agent 的工作
3. 不违反核心约束（类型定义、禁止事项）
4. 可以更新 PROJECT_SPEC.md 以反映新结构

**示例：**
```
规范要求：src/data/diaryService.ts
实际实现：src/storage/diaryStorage.ts

判断：
- 将 localStorage 相关函数放在 storage 目录更清晰
- 不影响其他 Agent（他们只需要导入函数）
- 不违反核心约束
- 结论：✅ 可以接受，更新 PROJECT_SPEC.md
```

### 8.3 优先级判断

**高优先级（必须修改）：**
- 违反禁止事项
- 类型定义冲突
- Agent 越界
- 功能缺失

**中优先级（建议修改）：**
- 文件位置不符合规范（但不影响功能）
- 函数命名不规范
- 缺少错误处理

**低优先级（可选优化）：**
- 代码可以更简洁
- 注释可以更清晰
- 性能可以优化

### 8.4 沟通技巧

**✅ 好的反馈：**
```
问题：DiaryCard 组件中直接操作了 localStorage
位置：src/components/diary/DiaryCard.tsx:42
建议：删除直接操作，改为调用 diaryStorage.createEntry()
理由：根据 PROJECT_SPEC.md 第 10.3 节，禁止直接操作 localStorage
优先级：高（必须修改）
```

**❌ 不好的反馈：**
```
这个代码写得不好，需要重写。
```

### 8.5 更新 PROJECT_SPEC.md 的时机

**应该更新规范的情况：**
1. Agent 的实现明显更合理
2. 发现规范中的矛盾或遗漏
3. 需要补充新的约束或说明

**不应该更新规范的情况：**
1. Agent 只是偷懒不想按规范实现
2. 改动会影响其他 Agent 的工作
3. 违反了核心设计原则

---

## 9. 快速检查清单

在开始详细检查前，可以先用这个快速清单做初步判断：

### 快速检查（5 分钟）

- [ ] 文件数量是否正确
- [ ] 文件位置是否大致符合规范
- [ ] 是否有明显的越界文件（如数据 Agent 创建了 UI 组件）
- [ ] 是否使用了 `any` 类型（全局搜索 `: any`）
- [ ] 是否有明显的硬编码
- [ ] 是否违反了明显的禁止事项

**如果快速检查发现严重问题，可以直接指出，无需详细检查。**

---

## 10. 总结

作为代码检查 Agent，你的核心职责是：

1. **严格但不死板** - 基于规范检查，但接受合理的实现差异
2. **客观且建设性** - 提供具体的修改建议，而不是简单否定
3. **区分优先级** - 明确哪些必须修改，哪些可以优化
4. **保持一致性** - 确保所有 Agent 的代码风格和结构一致
5. **不越权** - 只检查和建议，不主动修改代码

**记住：你的目标是帮助项目顺利推进，而不是阻碍开发。**

---

## 附录：相关文档

- **PROJECT_SPEC.md** - 项目规范文档（必读）
- **src/types/diary.ts** - 核心类型定义（必读）
- **src/data/tags.ts** - 固定标签列表（必读）

---

**文档版本：** v1.0  
**最后更新：** 2026-05-16  
**维护者：** 总控 Agent  
**适用对象：** 代码检查 Agent








